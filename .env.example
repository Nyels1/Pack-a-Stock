# ===========================================
# PACK-A-STOCK - BACKEND ENVIRONMENT VARIABLES
# ===========================================

# -----------------
# Django Core
# -----------------
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,*.ngrok.io

# -----------------
# Database PostgreSQL
# -----------------
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pack_a_stock_db
DB_NAME=pack_a_stock_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# -----------------
# CORS (Frontend URLs)
# -----------------
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# -----------------
# JWT Settings
# -----------------
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

# -----------------
# File Storage - Cloudflare R2 (🆓 GRATIS - Recomendado)
# -----------------
# OPCIÓN GRATIS: Cloudflare R2 - 10GB gratis sin tarjeta
# Tutorial completo en: STORAGE_GRATIS.md

# Dejar USE_S3=False para desarrollo local (archivos en /media)
# Cambiar a True para usar Cloudflare R2 gratis
USE_S3=False

# CLOUDFLARE R2 (🆓 10GB GRATIS):
# 1. Crear cuenta: https://dash.cloudflare.com
# 2. R2 → Create bucket → nombre: pack-a-stock
# 3. Manage R2 API Tokens → Create API token → copiar keys
# 4. Settings → S3 API → copiar endpoint
# 5. Settings → Public Access → Allow Access → copiar URL pública

# AWS_ACCESS_KEY_ID=tu-r2-access-key-id
# AWS_SECRET_ACCESS_KEY=tu-r2-secret-key
# AWS_STORAGE_BUCKET_NAME=pack-a-stock
# AWS_S3_ENDPOINT_URL=https://abc123.r2.cloudflarestorage.com
# AWS_S3_CUSTOM_DOMAIN=pub-xxxxxx.r2.dev  # URL pública del bucket
# AWS_DEFAULT_ACL=public-read

# ALTERNATIVAS DE PAGO:
# DigitalOcean Spaces ($5/mes): AWS_S3_ENDPOINT_URL=https://nyc3.digitaloceanspaces.com
# AWS S3 (variable): AWS_S3_REGION_NAME=us-east-1 (sin endpoint)

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=pack-a-stock-media
AWS_S3_ENDPOINT_URL=
AWS_S3_CUSTOM_DOMAIN=
AWS_DEFAULT_ACL=public-read

# -----------------
# Redis / Celery (opcional - para tareas asíncronas)
# -----------------
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/0

# -----------------
# Email Settings (opcional - para notificaciones)
# -----------------
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-app-password
